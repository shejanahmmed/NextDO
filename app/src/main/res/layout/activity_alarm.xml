<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    android:padding="24dp"
    android:background="@color/alarm_background_color">

    <ImageView
        android:id="@+id/alarm_icon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"
        android:src="@drawable/ic_alarm"
        app:tint="@color/alarm_text_color" />

    <TextView
        android:id="@+id/alarm_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/alarm_icon"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:text="ALARM"
        android:textColor="@color/alarm_text_color"
        android:textSize="16sp"
        android:textStyle="bold"
        android:letterSpacing="0.1" />

    <TextView
        android:id="@+id/alarm_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/alarm_label"
        android:layout_marginTop="32dp"
        android:gravity="center"
        android:text="Task Reminder"
        android:textColor="@color/alarm_text_color"
        android:textSize="28sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/alarm_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/alarm_title"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:textColor="@color/alarm_text_color"
        android:textSize="16sp"
        android:alpha="0.8"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="32dp"
        android:orientation="horizontal"
        android:gravity="center">

        <Button
            android:id="@+id/btn_snooze"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:text="Snooze"
            android:textSize="16sp"
            android:backgroundTint="@color/alarm_snooze_bg_color"
            android:textColor="@color/alarm_snooze_text_color" />

        <Button
            android:id="@+id/btn_done"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Done"
            android:textSize="16sp"
            android:backgroundTint="@color/alarm_dismiss_bg_color"
            android:textColor="@color/alarm_dismiss_text_color" />

    </LinearLayout>

</RelativeLayout>
